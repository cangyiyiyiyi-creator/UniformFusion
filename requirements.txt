# Uniform Fusion — 论文环境依赖
#
# 论文所用版本（conda 环境 v2b384_env，Python 3.10.20）。
# 完整 81 包清单见 pip_freeze.txt；本文件只列出复现实验必需的核心依赖。
#
# 安装（CUDA 12.8 对应的 PyTorch 轮子需指定 index-url）：
#   pip install torch==2.9.0 torchvision==0.24.0 \
#       --index-url https://download.pytorch.org/whl/cu128
#   pip install -r requirements.txt
#
# 说明：论文全部实验只用 resnet50 / convnextv2_tiny / maxvit 主干与 BCE 多标签头，
# 不需要语义分支相关依赖（transformers / sentence-transformers 等），
# 因此这里未列出；如需开启 --use_semantic_branch true 请按 pip_freeze.txt 补齐。

# ---- 深度学习核心 ----
torch==2.9.0
torchvision==0.24.0
timm==1.0.22
einops==0.8.2
safetensors==0.6.2

# ---- 数值与数据处理 ----
numpy==2.2.6
pandas==2.3.3
scipy==1.15.3
scikit-learn==1.7.2
Pillow==12.0.0
opencv-python==4.12.0.88
PyYAML==6.0.3

# ---- 结果汇总与图表 ----
matplotlib==3.10.7
openpyxl==3.1.5
tqdm==4.67.1
