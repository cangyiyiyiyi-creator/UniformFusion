# UniformFusion — paper environment dependencies
#
# Versions used in the paper (conda environment v2b384_env, Python 3.10.20).
# The complete 81-package list is in pip_freeze.txt; this file only pins the
# core dependencies needed to reproduce the experiments.
#
# Installation (PyTorch wheels for CUDA 12.8 must come from the cu128 index):
#   pip install torch==2.9.0 torchvision==0.24.0 \
#       --index-url https://download.pytorch.org/whl/cu128
#   pip install -r requirements.txt
#
# Note: every experiment in the paper uses a resnet50 / convnextv2_tiny / maxvit
# backbone with a BCE multi-label head. The semantic-branch dependencies
# (transformers, sentence-transformers, ...) are therefore not listed here.
# If you enable --use_semantic_branch true, install them from pip_freeze.txt.

# ---- deep learning core ----
torch==2.9.0
torchvision==0.24.0
timm==1.0.22
einops==0.8.2
safetensors==0.6.2

# ---- numerics and data handling ----
numpy==2.2.6
pandas==2.3.3
scipy==1.15.3
scikit-learn==1.7.2
Pillow==12.0.0
opencv-python==4.12.0.88
PyYAML==6.0.3

# ---- result summarisation and plots ----
matplotlib==3.10.7
openpyxl==3.1.5
tqdm==4.67.1

